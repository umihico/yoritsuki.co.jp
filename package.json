{
  "name": "yoritsuki",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-unused-css.mjs",
    "build:local": "PUBLIC_SITE_URL=http://localhost:8642 astro build && node scripts/check-unused-css.mjs",
    "check:css": "node scripts/check-unused-css.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
