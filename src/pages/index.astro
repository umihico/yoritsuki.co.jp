---
import Layout from '../layouts/Layout.astro';
import HomeMain from '../components/HomeMain.astro';
import { HOME_META } from '../lib/company.js';
import { getLocale, pick } from '../lib/i18n.js';
const locale = getLocale(Astro);
---
<Layout title={pick(locale, HOME_META.title)} description={pick(locale, HOME_META.description)}>
  <HomeMain />
</Layout>
